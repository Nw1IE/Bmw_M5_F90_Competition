[package]
name = "m5_landing"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.7"
tokio = { version = "1.0", features = ["full"] }
tera = "1.19"
serde = { version = "1.0", features = ["derive"] }
tower-http = { version = "0.5", features = ["fs"] } # Добавь эту строку